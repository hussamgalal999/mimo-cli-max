# إصلاح خطأ "Invalid count value" ✅

## المشكلة
```
❌ Unhandled Promise Rejection: Invalid count value: -5
```

## السبب
كانت حسابات المسافات في التخطيط تعطي قيماً سالبة عند استخدام `repeat()`.

### الكود القديم (المشكلة):
```typescript
// هذا يمكن أن يعطي قيمة سالبة!
' '.repeat(W - title.length - 15)
' '.repeat(W - info.length - 2)
```

## الحل
استخدام `Math.max(0, ...)` لضمان عدم الحصول على قيم سالبة:

```typescript
// الآن دائماً >= 0
const titlePadding = Math.max(0, W - titleText.length - statusText.length - 4);
' '.repeat(titlePadding)
```

## التغييرات
- ✅ تبسيط حسابات التخطيط
- ✅ استخدام `Math.max(0, ...)` لجميع المسافات
- ✅ تقصير النصوص لتناسب العرض

## اختبر الآن
```bash
npx tsx bin/mimo.ts
```

يجب أن يعمل بدون أخطاء! 🎉
